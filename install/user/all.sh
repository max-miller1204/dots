# Sourced by dots-install: per-user runtime steps that config seeding can't
# express: anything needing $HOME expansion or live system state (the
# omarchy install/user/all.sh pattern).

source "$DOTS_PATH/install/user/git.sh"
source "$DOTS_PATH/install/user/theme.sh"
